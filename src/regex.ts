export const RX_REMOVE_CREATURE = /.*(destroy|exile).*(all|target).*(creature).*/g;
export const RX_REMOVE_ARTIFACT = /.*(destroy|exile).*(all|target).*(artifact).*/g;
export const RX_REMOVE_ENCHANTMENT = /.*(destroy|exile).*(all|target).*(enchant).*/g;
export const RX_REMOVE_PLANESWALKER = /.*(destroy|exile).*(all|target).*(planes).*/g;
export const RX_REMOVE_PERMANENT = /.*(destroy|exile).*(all|target).*(permanent).*/g;
export const RX_TUTOR = /.*(search your library for).*/g;
export const RX_CARD_DRAW = /.*(draw).*(card).*/g;
